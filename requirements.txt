# Core Deep Learning
torch>=1.10.0
torchaudio>=0.10.0

# Audio Processing
librosa>=0.9.0
soundfile>=0.10.0
audioread>=2.1.9
pydub>=0.25.0

# Data Processing
numpy>=1.21.0
scipy>=1.7.0
pandas>=1.3.0

# TextGrid Parsing
textgrid>=1.5.0

# Utilities
tqdm>=4.62.0

# Optional: For visualization
matplotlib>=3.4.0
seaborn>=0.11.0

# Optional: For configuration management
PyYAML>=5.4.0

# Note: Montreal Forced Aligner (MFA) needs to be installed separately
# See installation guide: https://montreal-forced-aligner.readthedocs.io/en/latest/installation.html

# Note: FFmpeg is required for pydub
# Install via: apt-get install ffmpeg (Linux) or brew install ffmpeg (Mac)
